; Generated by dockerbundle 0.2.0. Do not edit; change docker-bundle.yml and regenerate.
;
; Programs are ordered by priority, derived from each recipe's base value refined by the
; depends_on graph. Ordering is not readiness: the entrypoint waits on readiness probes
; before the processes that need them.

[supervisord]
nodaemon=true
user=root
logfile=/var/log/supervisor/supervisord.log
logfile_maxbytes=10MB
logfile_backups=3
pidfile=/run/bundle/supervisord.pid
childlogdir=/var/log/supervisor

[unix_http_server]
file=/run/bundle/supervisor.sock
chmod=0700

[rpcinterface:supervisor]
supervisor.rpcinterface_factory = supervisor.rpcinterface:make_main_rpcinterface

; Lets `dockerbundle ps|restart|logs` drive individual services from outside the
; container without restarting the whole bundle.
[supervisorctl]
serverurl=unix:///run/bundle/supervisor.sock

[program:mysql]
command=mysqld --user=root
priority=10
autostart=true
autorestart=true
startsecs=1
startretries=3
stopsignal=TERM
stopwaitsecs=60
stopasgroup=true
killasgroup=true
environment=EXTERNAL_ACCESS="%(ENV_MYSQL_EXTERNAL_ACCESS)s"
; Output goes to the container's stdout unmodified. Piping it through a prefixing
; filter would make that filter the process supervisord tracks, corrupting exit codes
; and autorestart, so per-service views come from `dockerbundle logs mysql`
; against the copy in childlogdir instead.
stdout_logfile=/dev/fd/1
stdout_logfile_maxbytes=0
redirect_stderr=true

[program:php-fpm]
command=php-fpm -F -y /etc/php/fpm/php-fpm.conf
priority=30
; Released by the entrypoint once every service's own init script has finished.
autostart=false
autorestart=true
startsecs=1
startretries=3
stopsignal=QUIT
stopwaitsecs=30
stopasgroup=true
killasgroup=true
; Output goes to the container's stdout unmodified. Piping it through a prefixing
; filter would make that filter the process supervisord tracks, corrupting exit codes
; and autorestart, so per-service views come from `dockerbundle logs php-fpm`
; against the copy in childlogdir instead.
stdout_logfile=/dev/fd/1
stdout_logfile_maxbytes=0
redirect_stderr=true

[program:vue_nginx_vite_vue]
command=sh -c 'cd /var/www/vue_nginx_vite_vue && npm run dev -- --host 0.0.0.0 --port 5173'
priority=45
directory=/var/www/vue_nginx_vite_vue
; Released by the entrypoint once every service's own init script has finished.
autostart=false
autorestart=true
startsecs=1
startretries=3
stopsignal=TERM
stopwaitsecs=15
stopasgroup=true
killasgroup=true
environment=EXTERNAL_ACCESS="%(ENV_VUE_EXTERNAL_ACCESS)s"
; Output goes to the container's stdout unmodified. Piping it through a prefixing
; filter would make that filter the process supervisord tracks, corrupting exit codes
; and autorestart, so per-service views come from `dockerbundle logs vue_nginx_vite_vue`
; against the copy in childlogdir instead.
stdout_logfile=/dev/fd/1
stdout_logfile_maxbytes=0
redirect_stderr=true

[program:queue-laravel_nginx_laravel]
command=php /var/www/laravel_nginx_laravel/artisan queue:work --sleep=3 --tries=3 --max-time=3600
priority=60
directory=/var/www/laravel_nginx_laravel
; Released by the entrypoint once every service's own init script has finished.
autostart=false
autorestart=true
startsecs=1
startretries=3
stopsignal=TERM
stopwaitsecs=120
stopasgroup=true
killasgroup=true
; Scaled through the .env file. This program does not bind a port, so extra copies are
; safe; port-bound programs are deliberately left unscalable.
numprocs=%(ENV_LARAVEL_REPLICAS)s
process_name=%(program_name)s_%(process_num)02d
; Output goes to the container's stdout unmodified. Piping it through a prefixing
; filter would make that filter the process supervisord tracks, corrupting exit codes
; and autorestart, so per-service views come from `dockerbundle logs queue-laravel_nginx_laravel`
; against the copy in childlogdir instead.
stdout_logfile=/dev/fd/1
stdout_logfile_maxbytes=0
redirect_stderr=true

[program:nginx]
command=nginx -g "daemon off;"
priority=61
; Released by the entrypoint once every service's own init script has finished.
autostart=false
autorestart=true
startsecs=1
startretries=3
stopsignal=QUIT
stopwaitsecs=15
stopasgroup=true
killasgroup=true
; Output goes to the container's stdout unmodified. Piping it through a prefixing
; filter would make that filter the process supervisord tracks, corrupting exit codes
; and autorestart, so per-service views come from `dockerbundle logs nginx`
; against the copy in childlogdir instead.
stdout_logfile=/dev/fd/1
stdout_logfile_maxbytes=0
redirect_stderr=true

